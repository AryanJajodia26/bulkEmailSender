{
  "name": "email-sender-root",
  "version": "1.0.0",
  "description": "Root package for email sender application",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "install-deps": "cd backend && npm install"
  }
} 